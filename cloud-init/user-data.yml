#cloud-config
package_update: true
packages:
  - qemu-guest-agent
  - python3
  - curl
  - ca-certificates
  - git

ssh_pwauth: false
disable_root: true

runcmd:
  - systemctl enable --now qemu-guest-agent

