# Copia este archivo como .env y cambia el valor.
# Nunca versiones el .env real: comparte la variable GITEA_DB_PASSWORD
# con ansible/roles/gitea (Ansible Vault o inventario gitignored).
GITEA_DB_PASSWORD=change-me